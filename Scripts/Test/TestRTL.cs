using System.Collections;
using System.Collections.Generic;
using System.Linq;
using I2.Loc;
using RTLTMPro;
using TMPro;
using UnityEngine;

public class TestRTL : MonoBehaviour
{

    public string text;
	// Use this for initialization
	void Start ()
    {
  //      var comp = GetComponent<TextMeshProUGUI>();
		//RTLSupport rtl = new RTLSupport();
  //      rtl.PreserveNumbers = true;
  //      rtl.FixTextTags = true;
  //      rtl.Farsi = true;
  //      var loc = rtl.FixRTL(text);
  //      var reverse = loc.Reverse().ToArray().ArrayToString();
  //      reverse = reverse.Replace("}0{", "{0}").Replace("}1{", "{1}").Replace("}2{", "{2}").Replace("}3{", "{3}");
  //      Debug.Log(reverse);
		//var format = string.Format(reverse, "4");
  //      Debug.Log(format);
  //      comp.text = format;
    }
	
	// Update is called once per frame
	void Update () {
		
	}
}
